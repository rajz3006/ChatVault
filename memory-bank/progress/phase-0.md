# Phase 0: Project Scaffolding

## Status: NOT STARTED

## Tasks

- [ ] 1. Create directory structure (`chatvault/`, `chatvault/connectors/`, `chatvault/llm/`, `data/`)
- [ ] 2. Create `__init__.py` files (chatvault, connectors, llm)
- [ ] 3. Create `pyproject.toml` with metadata and dependencies
- [ ] 4. Create `.env.example` with documented env vars
- [ ] 5. Create `.gitignore`
- [ ] 6. Create `data/.gitkeep`
- [ ] 7. Create `run.sh` (basic launcher)
- [ ] 8. Create minimal `README.md` placeholder

## Verification
- `python -c "import chatvault"` succeeds
- `pip install -e .` installs the package
